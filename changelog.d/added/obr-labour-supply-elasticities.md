Add `LabourSupplyParams` to the parameter struct, encoding the OBR Slutsky-decomposition elasticities (substitution and income effects segmented by gender, marital status, and youngest-child age). Defaults to enabled, so labour supply responses are on by default; set `enabled=false` or override individual elasticities via the reform overlay. Also exposed in the Python wrapper's `Parameters` model.
