# Reform: Combined — raise PA + cut NI + expand UC
#
# Demonstrates modifying multiple policy areas in a single reform.
#
# Run with: cargo run --release -- --reform reforms/combined_example.yaml

income_tax:
  personal_allowance: 15000.0

national_insurance:
  main_rate: 0.06

universal_credit:
  taper_rate: 0.50
